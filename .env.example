# Example configuration only. Never commit real credentials.
# Copy to .env when application configuration is implemented.
# No application currently reads these variables.
DATABASE_URL=
RABBITMQ_URL=
ASSET_STORAGE_PROVIDER=local

AI_PROVIDER=mock
MODEL_ID=
WORKER_CONCURRENCY=1
